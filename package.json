{
  "name": "zombie-collab-game",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "eslint": "^8.48.0",
    "prettier": "^3.0.3",
    "turbo": "latest"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
